# Environment Variables Template for Creator Tool MVP v0.1
# 
# Instructions:
# 1. Copy this file to .env (cp env.example .env)
# 2. Fill in your actual API keys
# 3. NEVER commit .env to version control
#
# Note: This is a template - do not put real API keys here

# ==================== Phase 2: Image Generation API ====================
# Image generation API key (for character and location images)
# Options: Google Imagen, OpenAI DALL-E, Stability AI, etc.
IMAGE_API_KEY=your_image_api_key_here
IMAGE_API_ENDPOINT=https://api.example.com/v1/images/generate
IMAGE_API_PROVIDER=google  # Options: google, openai, stability, custom

# Optional: Image generation settings
IMAGE_GENERATION_MODEL=imagen-3
IMAGE_GENERATION_SIZE=1024x1024
IMAGE_GENERATION_QUALITY=standard  # Options: standard, hd

# ==================== Phase 5: Video Generation API ====================
# Video generation API key (for video segments)
# Options: Google Imagen Video, RunwayML, Pika Labs, etc.
VIDEO_API_KEY=your_video_api_key_here
VIDEO_API_ENDPOINT=https://api.example.com/v1/videos/generate
VIDEO_API_PROVIDER=google  # Options: google, runway, pika, stable, custom

# Optional: Video generation settings
VIDEO_GENERATION_MODEL=imagen-video-1
VIDEO_GENERATION_DURATION=8  # Must be 8 seconds (Phase 5 constraint)
VIDEO_GENERATION_RESOLUTION=720p  # Options: 480p, 720p, 1080p

# ==================== Phase 5.5: Video Processing ====================
# Video stitching/processing (usually local, no API key needed)
# Options: FFmpeg (local), MoviePy (local), Cloud service
VIDEO_PROCESSING_ENGINE=ffmpeg  # Options: ffmpeg, moviepy, cloud
FFMPEG_PATH=/usr/bin/ffmpeg  # Path to FFmpeg binary (if using FFmpeg)

# Optional: Cloud video processing (if using cloud service)
# VIDEO_PROCESSING_API_KEY=your_video_processing_api_key_here
# VIDEO_PROCESSING_ENDPOINT=https://api.example.com/v1/videos/stitch

# ==================== General API Settings ====================
# API timeout settings (in seconds)
API_TIMEOUT=300  # 5 minutes for video generation
API_RETRY_MAX=3  # Maximum retry attempts
API_RETRY_BACKOFF=2  # Exponential backoff multiplier

# Rate limiting
API_RATE_LIMIT_PER_MINUTE=10  # Adjust based on provider limits
API_RATE_LIMIT_PER_HOUR=100

# Cost monitoring (optional)
# COST_ALERT_THRESHOLD=100  # Alert when cost exceeds this amount
# COST_MONITORING_ENABLED=true

# ==================== Environment ====================
# Environment: development, staging, production
ENVIRONMENT=development

# Enable/disable mock APIs (for development)
USE_MOCK_APIS=true  # Set to false when ready to use real APIs

# ==================== Storage (Optional) ====================
# If using cloud storage for images/videos
# STORAGE_TYPE=local  # Options: local, s3, gcs
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_S3_BUCKET=your_bucket_name
# GCS_PROJECT_ID=your_gcs_project_id
# GCS_BUCKET_NAME=your_gcs_bucket_name

# ==================== Logging & Monitoring (Optional) ====================
# LOG_LEVEL=INFO  # Options: DEBUG, INFO, WARNING, ERROR
# ENABLE_API_LOGGING=true
# MONITORING_ENABLED=false

